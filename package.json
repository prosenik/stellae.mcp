{
  "name": "brain-stellae",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply brain-stellae --local",
    "migrate:remote": "wrangler d1 migrations apply brain-stellae --remote",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^4.20250409.0",
    "typescript": "^5.8.3",
    "vitest": "~3.0.9",
    "wrangler": "^4.10.0"
  }
}
