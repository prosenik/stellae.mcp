{
  "name": "brain-stellae",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 execute brain-stellae --local --file=./schema.sql",
    "migrate:remote": "wrangler d1 execute brain-stellae --remote --file=./schema.sql"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250409.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.10.0"
  }
}
